# 11 · 已知坑与待修

> 本文件只列**当前基线**下确认存在的问题。修掉一条就删一条，别在这里堆历史。

## 反模式

| 位置          | 情况                                                                                                                                                                  |
| ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/App.vue` | 开局向导的显示判断在 `computed` 内写状态。`vue/no-side-effects-in-computed-properties` 已降级为 warning。属真实反模式，但重构涉及核心开局逻辑，**单独跟进**，别顺手改 |

## 构建 / 发布

| 情况                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `release.yml` 只把 `dist/index.html` 发到 GitHub Release，**不含** `dist/assets/`（图片音频）与 `dist/preset-package/`。从 Release 单独下载这个 HTML 打开会缺图、也加载不到预设。当前正式分发走 Cloudflare Pages 整目录部署，不受影响 |

## 检查盲区

| 情况                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 测试文件不在 `tsconfig.json` 的 `include` 里，`vue-tsc` 查不出测试文件里的「重复函数实现」这类错误——只能靠运行发现                                                                                                                                                                                                                                                                                            |
| 仓库 `format:check` 现在**全绿**。`.prettierignore` 排除了四类非代码资产：`src/assets/standalone-worldbooks/`（EJS 模板世界书 —— 格式化会重排模板内 JS 代码，实测一个文件改 683 行）、`src/presets/ws/*.json` 与 `src/诸界穿越预设.json`（手工维护的玩家预设数据）、`.github/ISSUE_TEMPLATE/`、`legacy-reference/`（旧实现存档）。**新增这类内容文件时要同步补进 ignore**，否则 `format:check` 会被它们带失败 |

## i18n

- 当前 `pnpm check:i18n` 结果为 **PASS**，三张表 key 一一对应、占位符一致；
- 但有 **1 条**「英文块残留中文值」的 WARN。详细条目见 `Temp/i18n-report.txt`（该目录不进仓库，需要时重新跑一次脚本生成）。

## 其他

- 全局变量垫片（`src/standalone-globals.ts`）必须保持是 `src/index.ts` 的**第一个 import**。
  顺序错会在初始化阶段就炸，且报错信息不指向真正原因。
- 通知容器必须挂在 `App.vue` 根节点，否则开局向导阶段弹不出提示。
