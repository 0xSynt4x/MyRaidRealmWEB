# pnpm workspace / project settings.
#
# @parcel/watcher is a transitive optional native dependency (via tailwind/sass
# tooling). Its build script is not required for building this project, so we
# explicitly opt out to avoid pnpm's "ignored build scripts" hard failure in CI.
onlyBuiltDependencies: []
