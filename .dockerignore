# 构建上下文排除清单。
#
# 没有这份文件，docker build 会把 node_modules（几十万文件）和 .git
# 一起打包发给 daemon，构建会慢到难以忍受。

node_modules
dist
.git
.gitignore

# 本地试验副本：改动与主仓库无关，不需要进镜像
sandbox

# e2e 测试不参与应用构建 —— 它测的是构建产物，不是构建的一部分。
# 排除掉还能避免把根用户创建的 node_modules 拷进构建上下文。
e2e

# 本地临时目录与工具缓存
Temp
.playwright-mcp
.sisyphus
.workbuddy-ai
.idea
.omx

# 编辑器与系统垃圾
.DS_Store
Thumbs.db
*.log
