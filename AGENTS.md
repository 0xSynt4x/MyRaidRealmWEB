# AGENTS.md

> 给 AI 编码代理的工作准则。**本文件只讲「怎么干活」，不讲项目技术细节。**
> 项目知识在 [`spec/`](./spec/README.md)：先用 `spec/README.md` 定位，再打开对应的 `spec/NN-*.md`，最后以实际代码确认。

## 核心原则

- 这是**单项目**（一个自包含的浏览器端产物），上下文相对稳定。优先在当前仓库内完成工作，不主动扩展到外部资料、无关模块或后续优化。
- 用户当前明确指令 > 本文件规则 > 其他默认偏好。
- 用简体中文回复；代码、命令、文件路径与技术标识保持英文。
- 先给结论和实际影响，再给必要证据。**但结论不能只有结论**——结论后面要跟上依据，否则用户无法判断对错。
- 以正确完成当前目标为第一优先级，同时减少无效搜索、重复读取、重复推理、无关修改和过度验证。
- 不为了省 token、省时间或少改几行而牺牲正确性、架构一致性或数据安全。

## 项目上下文

- 🔴 **本仓是独立网页游戏，不是任何外部工具的附属。** 已从 SillyTavern（酒馆）完全独立，
  `legacy-reference/` 只是旧实现存档，**永远不作为入口**。
- 遇到陌生模块、跨模块修改或需要理解数据流时，**先读 `spec/README.md` 的索引表定位**，
  再打开对应细则，最后读实际代码。不要默认通读 `spec/`。
- 文档用于快速建立项目地图；**实际代码是最终依据**。文档与代码不一致时以代码为准，
  并在影响当前任务时顺手把文档改回来。
- 保持项目已有的架构原则、状态所有权与数据流语义。除非当前任务明确要求，
  不主动替换架构或做大规模重构。

## 协作方式（本仓特有）

- 🔴 **改代码前先讲后动。** 任何增 / 删 / 改（包括删死代码、抽公共函数、改配置、改目录结构）
  先用大白话讲清「改什么 / 为什么 / 影响哪个功能」，**等用户点头再动手**。
  任务描述里出现「删除」「合并」「重构」也不等于已获授权。
  纯只读操作（读文件、搜索、跑类型检查）不受此限。
- 🔴 **讲用途，不讲名字。** 说「这段代码干嘛的、改完哪个功能会变」，
  不要甩函数名 / 变量名 / 字段名。精确的名字放进代码块，或等用户追问。
- 🔴 **给建议只说「改成什么」。** 清单类输出每条只留动作，现状与推导收进「依据」。
- 🔴 **一次只推进一项。** 讲清 → 等拍板 → 动手 → 报告，不要一口气铺开好几件事。
- 🔴 **视觉 / 界面改动做完直接给用户看，不要自己截图反复复查。** 判据在用户眼睛里，不在你的截图里。
  允许为定位 bug 做**单次**针对性探测；不要做多视口 × 多帧 × 拼图比对的例行验收。
- 🔴 **工作区改动不提交、不推送。** 除非用户明确要求发布，否则不要 `git commit` / `git push`，
  也不要问「要不要提交」。
- 对外发布一律用 `MyRaidRealm` 这类对外名称，**不得出现内部目录名**。
- 需求含糊时先问清目标 / 范围 / 约束，**最多 3 个问题**，不要靠猜。

## 自主执行

- 从指令和已有上下文推断实际目标与范围，倾向行动并持续推进，直到目标得到可检查、可使用的结果。
- 对不会改变需求语义、架构方向、用户数据或最终行为的**常规实现细节**，
  自行采用与现有代码最一致的方案，不为此停下询问。
- 只有缺失信息会实质改变结果、架构方向，或操作具有破坏性 / 不可逆性 / 明显数据兼容风险时才询问。
- 可逆、影响小的改动直接执行；破坏性或不可逆操作前必须确认。
- 用户批准的是**具体、可检查的结果**，不是空计划。能直接实施时不要反复停留在复述与方案讨论。
- 当用户的建议明显不利于目标时直接指出，不迎合。
- 当前目标完成后停止，不主动扩展到相邻问题、潜在优化或新功能。

## 搜索与阅读

- 开始修改前，先定位最可能相关的入口、符号、调用链和数据结构。
- 🔴 **搜索「先窄后宽」**：优先搜明确的函数名、类型、路径、错误信息、调用点；
  只有现有证据不足时才扩大范围。
- 已有足够证据能安全实施时，**停止搜索并进入修改**。
- 不为「完整理解项目」扫描整个仓库；不重复读取内容未变、上下文仍足够的文件。
- 大文件优先搜索目标符号并只读局部上下文，不默认整文件读取。
- 独立的搜索 / 读取 / 查询**尽量批量执行**，避免大量连续的小型工具调用。
- 不收集不会改变当前实现决策的信息。

## 代码与架构

- 修改前快速理解相关模块的现有模式、类型、调用关系与状态所有权，并保持一致。
- 优先**最小必要改动**：能够正确解决问题且保持架构一致的最小改动。
  不要为了减少改动行数而复制逻辑、绕过已有抽象或制造技术债。
- 避免与当前任务无关的重构、清理、格式化、抽象升级、依赖替换或风格调整。
- 涉及构建产物、状态同步、存档、提示词链路、变量更新时，
  **保留现有的作用域守卫、快照与竞态防护语义**。
- 不修改与当前任务无关的用户代码，即使发现可以顺手优化。
- 临时文件、调试代码和一次性脚本用完立即清理。

## 测试与验证

- 验证范围必须与改动的规模、影响范围和风险相称。
- 优先跑最相关、最小范围的检查（类型检查 / 单测 / 关键路径）。
- 对可逆、低影响、与现有模式高度一致的小改动，不主动编写没有验证价值的新测试。
- 对跨模块数据流、状态同步、存档、提示词链路等高耦合修改，**必须验证直接受影响的关键路径**。
- 已有检查通过且没有新的具体疑点后，停止扩大或重复测试。
- 验证失败时先按失败信息定位原因，**不要立刻扩大为全项目排查**。
- 不主动修复与当前任务无关的既有失败；确认无关后简要记录即可。
- 🔴 **「CI 绿」不等于「测试全过」。** 判据是日志里的通过条数与汇总行，不是退出码。

## 工具使用

- 搜索文件内容优先用专门的搜索工具，不要用 shell 的 `find` / `grep` 代替。
- 命令输出只取当前判断所需范围，避免无目的输出大型日志、完整文件、完整 diff 或整个目录树。
- 工具已经给出足够信息时，不用另一个工具重复获取同样信息。
- 工具调用失败时先按错误信息修正调用，不机械重复同一命令。
- 独立命令尽量并行执行；有依赖关系的命令串行执行。

## 子 Agent

- 简单任务、单一调用链任务、强共享状态任务由当前 Agent 直接完成。
- 只有任务能真正独立拆分，且并行能明显省时或降低主 Agent 上下文负担时才用子 Agent。
- 不为「多一个视角」、重复验证或简单文件搜索启动子 Agent。
- 委派前明确范围、必要输入、预期输出和完成标准；子 Agent 只处理被委派的范围。
- 主 Agent 负责整合结果、解决冲突，并只对最终实现做必要验证。

## 沟通与输出

- 工作过程中以执行为主，不逐条播报常规的搜索、读取、修改和测试动作。
- 只在出现阻塞、需要用户决策、发现会实质改变方案的重要事实，或完成明显阶段性成果时同步。
- 用简洁连贯的段落；只有信息真正并列、需要比较或存在明确步骤时才用列表。
- 技术细节只保留对理解结论、判断风险、复现问题或继续工作有帮助的部分。
- 避免套话、重复总结、无意义术语、未经请求的方案对比和冗长过程描述。
- 需要用户决策时给出少量明确选项并说明推荐，不抛宽泛的开放式问题。
- 最终回复简洁说明：完成了什么、关键影响、做了哪些必要验证、仍存在的真实问题。
- 不重复用户已经知道的信息，不在结尾主动提出与当前任务无关的后续优化。
